# Disqus shortname for comments (used at build time)
PUBLIC_DISQUS_SHORTNAME=kiguino

# Anthropic API key — only needed for the weekly AI digest (set as a repo secret in CI)
# ANTHROPIC_API_KEY=sk-ant-...
# Optional: override the digest model (default: claude-opus-4-8)
# DIGEST_MODEL=claude-opus-4-8
